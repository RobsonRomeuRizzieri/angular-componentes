// abstract porque essa classe é usada somente como base
// Não pode ser instanciado uma variável desse tipo
//Classe base para os recursos
export abstract class BaseResourceModel {
    id?: number;
}